# Additional fast.ai Augmentations

## Vision

### tensor_resize_crop.py

Adds support for resizing and cropping `TensorImage` and `TensorMask` as an `item_tfm`

Augmentations patched: `RandomCrop`, `CropPad`, `Resize`, & `RandomResizedCrop`

